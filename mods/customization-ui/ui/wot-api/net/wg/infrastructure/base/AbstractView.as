package net.wg.infrastructure.base {
    import flash.display.MovieClip;

    // Compile-time stub of WoT's own AbstractView. Only the members this mod overrides or
    // calls are declared; at runtime the real class ships with the client and takes over.
    public class AbstractView extends MovieClip {
        public function AbstractView() {
            super();
        }

        protected function configUI():void {
        }

        protected function onDispose():void {
        }

        protected function nextFrameAfterPopulateHandler():void {
        }
    }
}
